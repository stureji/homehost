{
  "name": "homehost-server",
  "version": "1.0.0",
  "main": "./dist/server.js",
  "repository": "https://github.com/stureji/homehost",
  "author": "Erik Nord",
  "license": "none",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc",
    "clean-compile": "rimraf dist && tsc",
    "test": "tsc && ava",
    "clean-test": "rimraf dist && tsc && ava",
    "serve": "nodemon",
    "start": "node ./target/server.js"
  },
  "dependencies": {
    "@types/express": "^4.17.8",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "ava": "^3.13.0"
  },
  "ava": {
    "files": [
      "target/test/**/*"
    ]
  }
}
